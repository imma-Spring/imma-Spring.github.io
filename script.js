/*let images = [
    '../images/pong.svg',
    '../images/spaceinvaders.svg'
];

// Get a random image from the array
let image = images[Math.floor(Math.random() * images.length)%images.length];

// Get the element with the class "hero"
let item = document.querySelector("#hero");

// Set the background image
item.style.backgroundImage = `url(${image})`;*/